{
	"name": "obsidian-webhooks-v2",
	"version": "2.0.0",
	"description": "Receive webhooks and create/update notes in your vault",
	"main": "main.js",
	"scripts": {
		"dev": "bun build main.ts --outdir=. --target=node --watch --external obsidian --format=cjs",
		"build": "bun build main.ts --outdir=. --target=node --minify --external obsidian --format=cjs",
		"version": "node version-bump.mjs && git add manifest.json versions.json"
	},
	"keywords": [
		"obsidian",
		"plugin",
		"webhooks",
		"automation"
	],
	"author": "trashhalo",
	"license": "MIT",
	"devDependencies": {
		"obsidian": "latest",
		"@types/node": "^16.11.6"
	}
}